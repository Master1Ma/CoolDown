#include "net_task_manager.h"

namespace CoolDown{
    namespace Client{
        NetTaskManager::NetTaskManager()
        :TaskManager(){
        }
        NetTaskManager::~NetTaskManager(){
        }
    }
}
